#pragma once

class basicMath
{
  public:
    basicMath() = default;
    virtual ~basicMath() = default;
    int add(int, int);
    int subtract(int, int);
    int multiply(int, int);
    int divide(int, int);

  private:

};
